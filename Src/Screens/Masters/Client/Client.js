import React from "react";
import {
    View,
    SafeAreaView,
    StyleSheet
} from "react-native";

const Client = ({ navigation }) => {
    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({

})

export default Client